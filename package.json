{
  "name": "foolon-admin-monorepo",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "docker:compose:deploy": "cd ./api && pnpm run build && cd ../web && pnpm run build && cd ../ && docker-compose up -d --build",
    "prepare": "husky install",
    "format": "prettier --write \"api/src/**/*.ts\" \"api/test/**/*.ts\" \"web/src\""
  },
  "keywords": [],
  "author": "",
  "dependencies": {
    "husky": "^8.0.3",
    "lodash": "^4.17.21",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/lodash": "^4.17.0",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "typescript": "^5.3.3"
  }
}
